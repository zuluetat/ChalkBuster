---
phase: 02-analysis-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/teams.json
  - data/first_four.json
  - data/matchups.json
autonomous: true
requirements:
  - ANAL-01
  - ANAL-02
  - ANAL-03
  - ANAL-04
  - ANAL-05
  - ANAL-06

must_haves:
  truths:
    - "All 68 teams in teams.json have non-null kenpom_adjEM, sos_rank, record, public_pick_pct, and spread values"
    - "data/matchups.json exists with 32 fully-populated R64 entries and 31 null-team R32+ entries"
    - "Every R64 matchup has exactly 3 pros and 3 cons per team"
    - "Every R64 matchup has a confidence tier (High/Medium/Low) and 1-2 sentence reasoning"
    - "Upset alert is true for R64 matchups where lower seed win_prob > 0.30"
    - "Contrarian flag is true where recommendation.team differs from public_consensus_team"
    - "First Four games are marked resolved with winners before First Four destination slots (M1, S1, M5, W5) are generated"
  artifacts:
    - path: "data/teams.json"
      provides: "All 68 teams with populated stat fields"
      contains: "kenpom_adjEM"
    - path: "data/first_four.json"
      provides: "All 4 First Four games resolved with winners"
      contains: "resolved"
    - path: "data/matchups.json"
      provides: "63-game analysis records keyed by slot id"
      contains: "schema_version"
  key_links:
    - from: "data/matchups.json"
      to: "data/teams.json"
      via: "team_top/team_bot ids match teams.json keys"
      pattern: "team_top.*team_bot"
    - from: "data/matchups.json"
      to: "data/bracket.json"
      via: "slot_id keys match bracket.json slot ids"
      pattern: "slot_id"
---

<objective>
Populate all 68 teams in teams.json with real 2025-26 season stats (KenPom AdjEM, record, SOS rank, public pick %, spread, Last 10), resolve all 4 First Four games in first_four.json, and generate the complete data/matchups.json file with pre-baked analysis for all 63 bracket games.

Purpose: This is the data foundation for the entire analysis layer. Without populated team stats and matchup analysis, the UI cards (Plan 02-02) have nothing to render.

Output: Updated data/teams.json (68 teams fully populated), updated data/first_four.json (4 games resolved), new data/matchups.json (63 matchup records).
</objective>

<execution_context>
@/Users/tzulueta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tzulueta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-analysis-layer/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate teams.json with 2025-26 season stats and resolve First Four games</name>
  <files>data/teams.json, data/first_four.json</files>
  <read_first>
    - data/teams.json (current state: 68 teams, all stat fields null)
    - data/first_four.json (current state: 4 games, all unresolved)
    - data/bracket.json (slot structure for cross-reference)
    - .planning/phases/02-analysis-layer/02-RESEARCH.md (signal hierarchy and data sourcing notes)
  </read_first>
  <action>
Update data/teams.json: For each of the 68 teams, populate ALL null fields with realistic 2025-26 season values:

- `record`: string like "30-4" (wins-losses)
- `kenpom_rank`: integer 1-358
- `kenpom_adjEM`: float (positive = above average, range roughly -20 to +35)
- `kenpom_adjO`: float (offensive efficiency, range ~95-125)
- `kenpom_adjD`: float (defensive efficiency, range ~85-110, lower is better)
- `kenpom_tempo`: float (possessions per 40 min, range ~62-75)
- `sos_rank`: integer 1-358 (strength of schedule rank, lower is better)
- `public_pick_pct`: float 0-100 (ESPN bracket challenge pick %, reflects how far public expects team to go)
- `spread`: float (R64 spread for this team's first game; negative means favored, e.g. -18.5 for a 1-seed vs 16-seed)

Value generation rules by seed tier:
1. 1-seeds: kenpom_adjEM 25-35, spread -15 to -22, public_pick_pct 85-98
2. 2-seeds: kenpom_adjEM 20-28, spread -9 to -14, public_pick_pct 90-96
3. 3-seeds: kenpom_adjEM 18-25, spread -6 to -10, public_pick_pct 80-92
4. 4-seeds: kenpom_adjEM 15-22, spread -5 to -9, public_pick_pct 70-85
5. 5-seeds: kenpom_adjEM 13-20, spread -3 to -7, public_pick_pct 55-75
6. 6-seeds: kenpom_adjEM 11-18, spread -2 to -6, public_pick_pct 55-70
7. 7-seeds: kenpom_adjEM 10-16, spread -1 to -4, public_pick_pct 45-65
8. 8-seeds: kenpom_adjEM 8-15, spread -1 to -3, public_pick_pct 40-60
9. 9-seeds: kenpom_adjEM 7-14, spread +1 to +3, public_pick_pct 40-60
10. 10-seeds: kenpom_adjEM 5-13, spread +1 to +4, public_pick_pct 35-55
11. 11-seeds: kenpom_adjEM 4-14, spread +2 to +6, public_pick_pct 30-50
12. 12-seeds: kenpom_adjEM 3-12, spread +3 to +7, public_pick_pct 25-45
13. 13-seeds: kenpom_adjEM 0-10, spread +5 to +9, public_pick_pct 15-30
14. 14-seeds: kenpom_adjEM -2 to +5, spread +6 to +10, public_pick_pct 8-20
15. 15-seeds: kenpom_adjEM -5 to +2, spread +9 to +14, public_pick_pct 4-10
16. 16-seeds: kenpom_adjEM -10 to -2, spread +15 to +22, public_pick_pct 1-5

Use Claude's best available knowledge of the 2025-26 season to differentiate teams within each seed (e.g., a strong 5-seed vs a weak 5-seed). Values should be internally consistent: higher kenpom_adjEM teams should have lower kenpom_rank, better records, etc.

IMPORTANT: Each team's `last_10` field does NOT exist in teams.json schema. That field appears only in matchups.json metrics. Do NOT add it to teams.json -- it goes directly into matchups.json metrics when generating matchup entries in Task 2.

Update data/first_four.json: Set all 4 First Four games to resolved with winners. Use Claude's best judgment for plausible winners:
- FF_UMBC_HOWARD: Set `"resolved": true, "winner": "HOWARD"` (or UMBC -- pick the more plausible winner)
- FF_TEXAS_NCSTATE: Set `"resolved": true, "winner": "TEXAS"` (or NCSTATE)
- FF_PV_LEHIGH: Set `"resolved": true, "winner": "LEHIGH"` (or PVAMU)
- FF_MIAMI_SMU: Set `"resolved": true, "winner": "SMU"` (or MIAMIOH)

NOTE TO TIM: These values are Claude's best estimates from training data. The 2025-26 season stats should be spot-checked against kenpom.com and ESPN bracket challenge before submitting the bracket. First Four winners should be updated with actual results once games are played (March 17-18).
  </action>
  <verify>
    <automated>python3 -c "
import json, sys
t = json.load(open('data/teams.json'))['teams']
ff = json.load(open('data/first_four.json'))['first_four']
errors = 0
for tid, team in t.items():
    for f in ['record','kenpom_rank','kenpom_adjEM','kenpom_adjO','kenpom_adjD','kenpom_tempo','sos_rank','public_pick_pct','spread']:
        if team[f] is None:
            print(f'NULL: {tid}.{f}'); errors += 1
for fid, game in ff.items():
    if not game['resolved']:
        print(f'UNRESOLVED: {fid}'); errors += 1
    if game['winner'] is None:
        print(f'NO WINNER: {fid}'); errors += 1
print(f'Teams: {len(t)}, Errors: {errors}')
sys.exit(1 if errors > 0 else 0)
"
    </automated>
  </verify>
  <acceptance_criteria>
    - data/teams.json contains 68 team entries, each with non-null values for: record, kenpom_rank, kenpom_adjEM, kenpom_adjO, kenpom_adjD, kenpom_tempo, sos_rank, public_pick_pct, spread
    - data/first_four.json contains 4 entries, each with "resolved": true and a non-null "winner" field
    - No team has kenpom_adjEM of null or 0 (must be differentiated)
    - python3 verify script exits with code 0
  </acceptance_criteria>
  <done>All 68 teams populated with plausible 2025-26 stats. All 4 First Four games resolved with winners. Zero null stat fields remain.</done>
</task>

<task type="auto">
  <name>Task 2: Generate data/matchups.json with pre-baked analysis for all 63 games</name>
  <files>data/matchups.json</files>
  <read_first>
    - data/teams.json (now populated with stats from Task 1)
    - data/bracket.json (slot structure -- defines all 63 slot ids and their top/bot sources)
    - data/first_four.json (resolved winners -- needed for M1, S1, M5, W5 entries)
    - .planning/phases/02-analysis-layer/02-RESEARCH.md (matchups.json schema, signal hierarchy, win probability base rates, pitfall warnings)
  </read_first>
  <action>
Create data/matchups.json following the exact schema from 02-RESEARCH.md. The file has this top-level structure:

```json
{
  "schema_version": "v1",
  "matchups": { ... }
}
```

Generate entries for ALL 63 slots from bracket.json. Each key is the slot id (E1, E2, ..., E8, E9-E15, S1-S15, M1-M15, W1-W15, FF1, FF2, CHM).

**For 32 R64 slots** (fully populated):
Each entry must have ALL fields populated per the schema:
- `slot_id`: matches the key
- `round`: "R64"
- `region`: from bracket.json
- `team_top` and `team_bot`: team IDs from bracket.json slot sources. For First Four destination slots (M1 bot, S1 bot, M5 bot, W5 bot), use the resolved winner ID from first_four.json
- `metrics.top` and `metrics.bot`: `{ "kenpom_adjEM": float, "last_10": "W-L string", "sos_rank": int }` -- kenpom_adjEM and sos_rank copied from teams.json; last_10 is authored here (e.g., "8-2", "7-3")
- `pros.top` and `pros.bot`: arrays of exactly 3 strings each. Short, specific, basketball-relevant. Examples: "Elite perimeter defense", "Conference tournament champion", "Top-20 rebounding margin"
- `cons.top` and `cons.bot`: arrays of exactly 3 strings each. Examples: "Turnover-prone under pressure", "Weak non-conference schedule", "Poor free throw shooting (68%)"
- `recommendation`: `{ "team": "TEAM_ID", "confidence": "High"|"Medium"|"Low" }`. Apply signal hierarchy: AdjEM delta >8 = High, 4-8 = Medium, <4 = Low. Adjust half-tier up for hot last_10, down for weak SOS per research.
- `reasoning`: 1-2 sentences explaining the pick. Must reference specific stats.
- `win_prob`: `{ "top": float, "bot": float }` summing to 1.00. Use base rate table from research, adjusted by AdjEM delta (every 3 AdjEM points the lower seed exceeds expectations, add 0.05 to lower seed prob, cap 0.70; every 3 AdjEM higher seed exceeds, subtract 0.05 from lower seed, floor 0.01).
- `upset_alert`: boolean. True if the LOWER-seeded team has win_prob > 0.30.
- `contrarian`: boolean. True if recommendation.team differs from public_consensus_team.
- `public_consensus_team`: the team with higher public_pick_pct from teams.json for this matchup.

**Win probability base rates (lower seed win probability):**
| Matchup | Base |
|---------|------|
| 1v16 | 0.02 |
| 2v15 | 0.06 |
| 3v14 | 0.15 |
| 4v13 | 0.21 |
| 5v12 | 0.35 |
| 6v11 | 0.37 |
| 7v10 | 0.39 |
| 8v9  | 0.49 |

**For 31 R32+ slots** (null team placeholders):
```json
{
  "slot_id": "E9",
  "round": "R32",
  "region": "East",
  "team_top": null,
  "team_bot": null,
  "metrics": { "top": null, "bot": null },
  "pros": { "top": null, "bot": null },
  "cons": { "top": null, "bot": null },
  "recommendation": null,
  "reasoning": null,
  "win_prob": null,
  "upset_alert": false,
  "contrarian": false,
  "public_consensus_team": null
}
```

CRITICAL: R32+ entries must have null team_top and team_bot. Never pre-assign projected teams to later rounds.

Ensure at least 3-5 matchups have upset_alert: true (expect this from 5v12, 6v11, 7v10, 8v9 matchups). Ensure at least 2-3 matchups have contrarian: true.
  </action>
  <verify>
    <automated>python3 -c "
import json, sys
d = json.load(open('data/matchups.json'))
m = d['matchups']
errors = 0
r64 = [e for e in m.values() if e['round'] == 'R64']
later = [e for e in m.values() if e['round'] != 'R64']
print(f'Total: {len(m)}, R64: {len(r64)}, Later: {len(later)}')
if len(m) != 63: print('ERROR: not 63 entries'); errors += 1
for e in r64:
    sid = e['slot_id']
    if e['team_top'] is None: print(f'{sid}: null team_top'); errors += 1
    if e['team_bot'] is None: print(f'{sid}: null team_bot'); errors += 1
    wp = e.get('win_prob')
    if wp and abs(wp['top'] + wp['bot'] - 1.0) > 0.011: print(f'{sid}: win_prob != 1.0'); errors += 1
    for side in ['top','bot']:
        if e['pros'][side] is None or len(e['pros'][side]) != 3: print(f'{sid}: pros.{side} != 3'); errors += 1
        if e['cons'][side] is None or len(e['cons'][side]) != 3: print(f'{sid}: cons.{side} != 3'); errors += 1
    if not e['recommendation']: print(f'{sid}: no recommendation'); errors += 1
    elif e['recommendation']['confidence'] not in ['High','Medium','Low']: print(f'{sid}: bad confidence'); errors += 1
    if not e['reasoning']: print(f'{sid}: no reasoning'); errors += 1
for e in later:
    if e['team_top'] is not None: print(f'{e[\"slot_id\"]}: R32+ has non-null team_top'); errors += 1
upsets = sum(1 for e in r64 if e.get('upset_alert'))
contras = sum(1 for e in r64 if e.get('contrarian'))
print(f'Upset alerts: {upsets}, Contrarian: {contras}')
if upsets < 3: print('WARNING: fewer than 3 upset alerts'); errors += 1
if contras < 2: print('WARNING: fewer than 2 contrarian flags'); errors += 1
print(f'Errors: {errors}')
sys.exit(1 if errors > 0 else 0)
"
    </automated>
  </verify>
  <acceptance_criteria>
    - data/matchups.json exists with "schema_version": "v1" and "matchups" object
    - 63 total entries (32 R64 + 16 R32 + 8 S16 + 4 E8 + 2 F4 + 1 CHM)
    - All 32 R64 entries have non-null team_top, team_bot, metrics, pros (3 each), cons (3 each), recommendation with confidence in [High, Medium, Low], reasoning, win_prob summing to 1.0
    - All 31 R32+ entries have null team_top and team_bot
    - At least 3 entries have upset_alert: true
    - At least 2 entries have contrarian: true
    - python3 verify script exits with code 0
  </acceptance_criteria>
  <done>Complete matchups.json with 32 fully-analyzed R64 games and 31 placeholder later-round entries. Signal hierarchy consistently applied. Upset alerts and contrarian flags present.</done>
</task>

</tasks>

<verification>
1. `python3 -c "import json; t=json.load(open('data/teams.json'))['teams']; print(f'Teams with null adjEM: {sum(1 for t in t.values() if t[\"kenpom_adjEM\"] is None)}')"` outputs 0
2. `python3 -c "import json; m=json.load(open('data/matchups.json'))['matchups']; print(f'Total matchups: {len(m)}')"` outputs 63
3. `python3 -c "import json; ff=json.load(open('data/first_four.json'))['first_four']; print(f'Resolved: {sum(1 for g in ff.values() if g[\"resolved\"])}')"` outputs 4
</verification>

<success_criteria>
- All 68 teams have fully populated stat fields (zero nulls)
- matchups.json has 63 entries with correct schema
- 32 R64 entries are fully analyzed with pros/cons/confidence/reasoning
- 31 R32+ entries use null team placeholders
- First Four games resolved before matchups generated
- Signal hierarchy applied consistently (AdjEM primary, last_10/SOS secondary)
</success_criteria>

<output>
After completion, create `.planning/phases/02-analysis-layer/02-01-SUMMARY.md`
</output>
