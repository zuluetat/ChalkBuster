---
phase: 03-persistence-and-sharing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [PERS-01, PERS-02, SHAR-01, SHAR-02]
user_setup:
  - service: supabase
    why: "CORS origin must be added via Supabase dashboard (no CLI/API available)"
    dashboard_config:
      - task: "Add https://zuluetat.github.io to allowed origins"
        location: "Supabase Dashboard > Project Settings > API > Allowed Origins"

must_haves:
  truths:
    - "Supabase REST API accepts requests from https://zuluetat.github.io without CORS errors"
    - "share_token column has a DB-level default that auto-generates tokens on INSERT"
    - "Existing save/load/share/reset flows work end-to-end from GitHub Pages"
  artifacts: []
  key_links:
    - from: "https://zuluetat.github.io"
      to: "https://osayanpbjyndsnyxdwzh.supabase.co/rest/v1/brackets"
      via: "CORS allowed origin"
      pattern: "zuluetat.github.io"
---

<objective>
Verify Supabase configuration is production-ready for GitHub Pages deployment.

Purpose: The Supabase project is live and code is written, but CORS origins and share_token auto-generation have not been verified for production. This plan closes those configuration gaps so the app works from GitHub Pages, not just localhost.
Output: Confirmed working CORS + share_token configuration.
</objective>

<execution_context>
@/Users/tzulueta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tzulueta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-persistence-and-sharing/03-RESEARCH.md
@js/supabase.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Supabase share_token default and test existing flows</name>
  <files>js/supabase.js</files>
  <read_first>
    - /Users/tzulueta/ChalkBuster/js/supabase.js
    - /Users/tzulueta/ChalkBuster/js/app.js
    - /Users/tzulueta/ChalkBuster/js/state.js
  </read_first>
  <action>
    Run a test POST to the Supabase brackets table to verify share_token auto-generation:

    ```bash
    curl -s -X POST "https://osayanpbjyndsnyxdwzh.supabase.co/rest/v1/brackets" \
      -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im9zYXlhbnBianluZHNueXhkd3poIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM3OTA0MDcsImV4cCI6MjA4OTM2NjQwN30.e62mjOD4RrAemqbgMKwKJG__6SVDTBmJ4Y7x5D2-SBA" \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im9zYXlhbnBianluZHNueXhkd3poIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM3OTA0MDcsImV4cCI6MjA4OTM2NjQwN30.e62mjOD4RrAemqbgMKwKJG__6SVDTBmJ4Y7x5D2-SBA" \
      -H "Content-Type: application/json" \
      -H "Prefer: return=representation" \
      -d '{"picks": {"_test": true}}'
    ```

    Verify the response includes a non-null `share_token` value. If it does, the DB default is working.

    Then DELETE the test row using the returned `id`:

    ```bash
    curl -s -X DELETE "https://osayanpbjyndsnyxdwzh.supabase.co/rest/v1/brackets?id=eq.{RETURNED_ID}" \
      -H "apikey: ..." -H "Authorization: Bearer ..."
    ```

    Also verify that the existing `loadBracket()`, `savePicks()`, `loadSharedBracket()`, and `getShareURL()` functions in supabase.js are correctly implemented by reading the code. No code changes expected unless a bug is found.
  </action>
  <verify>
    <automated>curl -s -X POST "https://osayanpbjyndsnyxdwzh.supabase.co/rest/v1/brackets" -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im9zYXlhbnBianluZHNueXhkd3poIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM3OTA0MDcsImV4cCI6MjA4OTM2NjQwN30.e62mjOD4RrAemqbgMKwKJG__6SVDTBmJ4Y7x5D2-SBA" -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im9zYXlhbnBianluZHNueXhkd3poIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM3OTA0MDcsImV4cCI6MjA4OTM2NjQwN30.e62mjOD4RrAemqbgMKwKJG__6SVDTBmJ4Y7x5D2-SBA" -H "Content-Type: application/json" -H "Prefer: return=representation" -d '{"picks": {"_test": true}}' 2>&1 | grep -o '"share_token":"[^"]*"'</automated>
  </verify>
  <acceptance_criteria>
    - curl POST to brackets table returns HTTP 201 with a JSON body containing a non-null "share_token" field
    - The test row is deleted after verification
  </acceptance_criteria>
  <done>share_token auto-generation confirmed working via live API test</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Configure CORS for GitHub Pages in Supabase dashboard</name>
  <files>N/A - Supabase dashboard configuration</files>
  <read_first>
    - /Users/tzulueta/ChalkBuster/.planning/phases/03-persistence-and-sharing/03-RESEARCH.md
  </read_first>
  <action>
    This is a human-only action -- CORS origins must be configured in the Supabase web dashboard (no CLI/API available for this setting).

    Steps for the user:
    1. Go to https://supabase.com/dashboard and open the ChalkBuster project
    2. Navigate to Project Settings (gear icon) > API
    3. Under "Allowed Origins" (or "CORS Origins"), add: https://zuluetat.github.io
    4. Optionally also add: http://localhost:8000 (for local dev)
    5. Save the configuration

    After the user completes this, verify by opening https://zuluetat.github.io/ChalkBuster/ in Chrome DevTools Network tab, making a pick, and confirming the Supabase PATCH request returns 200 (not a CORS error).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" -X OPTIONS "https://osayanpbjyndsnyxdwzh.supabase.co/rest/v1/brackets" -H "Origin: https://zuluetat.github.io" -H "Access-Control-Request-Method: POST" -H "Access-Control-Request-Headers: apikey,authorization,content-type"</automated>
  </verify>
  <acceptance_criteria>
    - Supabase dashboard shows https://zuluetat.github.io in allowed origins list
    - OPTIONS preflight request from zuluetat.github.io origin returns 200 (not 403 or missing CORS headers)
  </acceptance_criteria>
  <done>CORS configured for GitHub Pages -- Supabase API accepts requests from https://zuluetat.github.io</done>
</task>

</tasks>

<verification>
- Supabase REST API responds to POST with auto-generated share_token
- CORS allows requests from https://zuluetat.github.io
- Existing save/load/share/reset code paths require no changes
</verification>

<success_criteria>
Supabase configuration is production-ready: CORS allows GitHub Pages origin, share_token auto-generates on row creation, and all existing persistence/sharing code works without modification.
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence-and-sharing/03-01-SUMMARY.md`
</output>
